# Phase 1 Audit — Run 35 — 2026-08-12

## Environment
Synced to `origin/claude/adoring-meitner-7xxgfq` at `b0989ac` (Run 34's tip). No reinstall
needed — 904 tests green before any changes, matching Run 34's final count exactly.

## Dead-code cleanup (done this run)
Confirmed via `grep -rn "voice_input"` across `app.py` and `modules/*.py` that
`modules/voice_input.py` (the pre-Run-34 streamlit-mic-recorder path) has **zero** remaining
call sites — Run 34's `web_speech.py` fully replaced it at both mic call sites and nothing else
imports it. It also had zero test coverage (no `tests/test_voice_input.py` exists), so removing
it doesn't change the pass count. Removed:
- `modules/voice_input.py` (43 lines)
- `streamlit-mic-recorder==0.0.8` from `requirements.txt`
- stale `voice_input.py` line from README's module tree; added the missing `web_speech.py` line
  that Run 34 never added

Shipped on `chore/remove-voice-input-dead-code`, merged `--no-ff` into
`claude/adoring-meitner-7xxgfq` before feature work started. 904 tests green after merge, live
`streamlit run` smoke test HTTP 200 with a clean log (no tracebacks).

## Corrected environment facts
- **Prism is a Streamlit app**, not a React/Next.js PWA — no `package.json`/`node_modules`
  anywhere in the repo (`app.py` + `modules/*.py`, Plotly for charts, custom CSS injected via
  `st.markdown(unsafe_allow_html=True)` for the glassmorphic look). This was already corrected in
  Run 34's audit; re-confirmed this run since the routine's own boilerplate text still assumes
  React. No frontend `npm install` step exists or is needed.
- `shap==0.49.1` is already a pinned dependency and already **fully wired up**:
  `modules/mllab.py::explain_with_shap()` / `shap_for_display()` exist and are called from
  `app.py`. Model explainability via SHAP is NOT a gap — it's already shipped (predates this
  routine's memory of it, or was added in a run whose changelog entry didn't use the word "SHAP"
  prominently). Confirmed via `grep -rln "import shap" modules/ app.py` →
  `modules/mllab.py`, `app.py`. This closes off one of the routine brief's suggested research
  angles before the research sweep even starts — don't re-propose SHAP.
- `imbalanced-learn==0.12.4` is also already pinned. Needs checking whether it's actually wired
  up beyond SMOTE (brief specifically flagged "imbalanced-learning beyond SMOTE" as a possible
  gap) — see research notes.

## Playwright verification (real browser, `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`)
Per Run 34's confirmed-working recipe (`executable_path=...`, never `playwright install`).
Screenshots: landing page (desktop, zero console/page errors), sample dataset loaded + Overview
tab rendered at desktop (1440x960) and mobile-PWA (390x844) widths, dark theme (default). Atlas
panel, data health ring, column table, and cleaning-tool accordion all render correctly at both
widths with no clipping or overflow. No new bugs found in this pass — the app is stable and this
is the 35th run building on 34 prior audits, so a full re-audit of every panel is not a good use
of this run's time budget; spot-checked the landing + Overview + Atlas surfaces (the highest-
traffic, most Recently-touched-by-prior-runs code) and moved to feature work.

## Conclusion
No genuinely new UI bugs found this pass. One real, confirmed, safe cleanup shipped (dead
voice_input.py + its now-unused pip dependency). Proceeding to Phase 2 research for the two
feature slots.

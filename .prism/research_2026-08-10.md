# Prism Research — 2026-08-10 (Run 3)

Light-pass research (prior runs already did a full four-class sweep twice
in the last 3 days; this run re-validates the still-open backlog items
against current sources rather than re-running the whole sweep from
scratch, per the "don't rebuild what's already been researched" spirit of
the routine's own memory rule).

## Ranked candidates

| Feature | Evidence | Depth (1-5) | Effort | Risk | Roadmap theme |
|---|---|---|---|---|---|
| Anomaly narration (Gemini explains flagged rows + suggests next action) | XAD (explainable anomaly detection) is an active research thread — "providing clear, human-understandable explanations for why specific data points are flagged... revealing which features contributed and how they deviate from expected behavior." [MDPI survey](https://www.mdpi.com/1424-8220/26/8/2330), [arXiv 2507.15676](https://arxiv.org/pdf/2507.15676) | 4 | S–M | Low (additive UI, existing rate-limited `call_gemini`, graceful no-model fallback) | **Agentic AI analysis (priority this cycle)** |
| Local Outlier Factor as a 2nd detection method | Density-based vs. tree-based is a standard applied-ML comparison interviewers probe directly; scikit-learn's own docs frame IsolationForest vs. LOF as the canonical pairing. [sklearn outlier detection docs](https://scikit-learn.org/stable/modules/outlier_detection.html), [comparison writeup](https://medium.com/@sidbhaumik1918/scalable-anomaly-detection-a-deep-dive-into-isolation-forest-vs-local-outlier-factor-c1bac72dc133) | 4 | S | Low (same module, same tests file, sklearn already a dependency) | ML capability / statistical rigor |
| Test-suite integrity fix (port `eval/*_eval.py` → real `tests/test_*.py`) | Not sourced externally — found via this run's own audit (`.prism/audit_2026-08-10.md`). Directly closes Run 1's own headline "no visible test coverage" finding, which Run 2 believed it had closed but hadn't. | n/a (hygiene, not a feature) | S | Very low (porting existing, already-passing assertions; no behavior change) | Reproducibility / interview credibility |
| `google-generativeai` → `google-genai` SDK migration | Deprecation warning fires on every import today; old SDK unmaintained. | 3 | M | Medium — touches every Gemini call site (`ai_analyst.py`, `auto_analyst.py`, `atlas.py`, `auto_insights.py` now too) | Maintenance, not a differentiator |
| polars/DuckDB unified large-file backend | Competitor tools (Hex, Deepnote) lean on DuckDB for big files; Prism's own SQL Lab already uses DuckDB, just not wired to the main pipeline. | 4 | L | Medium-high — architecture-adjacent, explicitly flagged by the routine's own guardrails as "no architecture rewrites without a dedicated run" | Ecosystem tech |
| Feature Selection Engine (mutual info / RFE / L1) for ML Lab | Standard ML Lab addition, high interview signal, but sizable net-new module + UI + eval surface | 4 | M | Low-Medium | ML capability |
| Data Quality Score exportable scorecard | Re-checked this run: `modules/report.py` + `modules/report_writer.py` already export health score, missing/outlier/duplicate breakdown, and a Gemini-written executive summary as HTML/PDF. Marking this **effectively already shipped** under a different name — remove from future backlogs unless a specific gap is found (e.g. a standalone JSON scorecard endpoint). | 2 | S | Low | — |

## This run's picks (reasoning in `.prism/routine_log.md`)

1. **Test-suite integrity fix** (not one of the 2-3 features, but shipped
   alongside them per the routine's "bugs found during audit are eligible
   as small fixes" rule) — highest leverage per token spent, zero behavior
   risk, directly serves interview credibility.
2. **Anomaly narration** — required pick for this cycle's mandatory
   agentic-AI-analysis theme; backlog item since Run 1; small, additive,
   mirrors an already-shipped, already-tested pattern (`auto_insights.
   narrate_insights`).
3. **Local Outlier Factor detection method** — pairs with #2 in the same
   UI expander and module, adds genuine statistical/ML breadth (density-
   based vs. tree-based outlier detection) for minimal incremental risk.

Not picked this run (stays on backlog, see audit file + routine log):
Feature Selection Engine, polars/DuckDB backend, SDK migration, DBSCAN
(dropped — LOF alone already covers "a second, meaningfully different
outlier method" without adding a third method's UI/test surface in one run).

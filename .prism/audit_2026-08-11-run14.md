# Audit — 2026-08-11, Run 14

Lightweight pass, same precedent as Runs 9, 12: this is the fourteenth cycle
run against this repo (thirteen same-day, one on 2026-08-07). A full app
walkthrough was last done fresh in Run 11 with nothing materially new
surfaced since (Runs 12-13 each reused it). Booted the app fresh this run
(`streamlit run app.py`, HTTP 200) and did a targeted pass around the area
touched by this run's feature (upload flow, Overview tab, Anomaly Detection
expander) rather than re-walking every tab.

**Targeted finding that shaped this run's feature selection:** of the eight
detectors the Agentic Insight Orchestrator now synthesizes across (see
Run 9-13 log entries), only two — `auto_insights` and
`confounder_detection.auto_scan_for_confounding` — actually run on upload.
The other six (anomaly ensemble, causal ATT/CATE, drift, hypothesis sweep,
the Auto Analyst verifier) are lazy: nothing computes until the user opens
that specific tab and clicks a button. That means a first-time visitor who
only glances at the Overview tab gets cross-detector synthesis from at most
2 of 8 possible sources — real orchestration depth is hidden behind clicks
the routine's own mandatory "auto-EDA on upload" theme says shouldn't be
required. This run closes that gap for the anomaly detector specifically
(see routine_log.md Run 14 for why anomaly and not another lazy detector).

No new bugs, dead UI, or styling regressions found outside the above. The
standing backlog (PyGWalker builder remaining scope, DuckDB/polars Auto
Cleaner path, light-theme dataframe repaint-lag) is unchanged — see
`research_2026-08-11-run14.md`.

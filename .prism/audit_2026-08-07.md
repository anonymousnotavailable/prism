# Audit — 2026-08-07

## Boot / health (via subagent + manual smoke test)
- [ok] `python -m py_compile` clean across app.py/modules/api.
- [ok] App boots via `streamlit run app.py`, no tracebacks, HTTP 200.
- [ok] No circular imports; `modules/ai_analyst.py` is the shared hub, one-directional.
- [ok] No bare `except:` anywhere; Gemini errors (quota/auth/other) are typed and
  surfaced with actionable messages via `ai_analyst.call_gemini`.
- [ok] Real per-session Gemini rate limiting (30 calls/hour/session), correctly
  a no-op outside a real browser session so eval/CI scripts aren't penalized.
- [ok] `ask_and_execute` self-healing retry on sandboxed pandas-code failure.
- [ok] Empty-dataframe/oversized-file handling: `MAX_ROWS`/`HARD_ROW_CEILING`
  truncation, 34 `.empty` guards across data_engine.py/app.py.
- [ok] `.gitignore` covers `.env` and `.streamlit/secrets.toml`; nothing stray tracked.
- [ok] Zero TODO/FIXME/HACK markers anywhere in the codebase.
- [ok] No automated tests existed before this run (confirmed via `find`).

## Feature gap (selected for this run)
- [gap] `modules/auto_analyst.py` performed **zero statistical significance
  testing** — its plan steps are LLM-generated pandas code, synthesized into
  prose findings with no p-values. `modules/stats_lab.py` has full
  significance-testing machinery but was never wired into the automated flow.
  → Closed this run via `modules/insight_verifier.py` (see CHANGELOG).

## New findings from this run's UI screenshot pass
- [risk] **Light theme ("Arctic") contrast/consistency gaps**, pre-existing,
  unrelated to this run's changes: the main `st.dataframe` grid keeps a dark
  background+white-text styling that doesn't switch with the Arctic theme;
  the segmented-control nav pills ("Overview/Clean/…") render with low-contrast
  text on a dark pill background in light mode; the Atlas "Ask by voice"
  button and the bottom chat input bar also stay dark-styled in light mode.
  Screenshots: `.prism/runs/2026-08-07/anomaly_narrated_desktop_light.png`,
  `auto_analyst_verified_section_light.png`. This is a real-world screenshot
  find, not simulated — worth a dedicated pass next run (likely a CSS
  selector scoped too narrowly to catch these components under the light
  token set).
- [risk] **Mobile viewport (390×844) layout**: at this width the Atlas chat
  column appears to claim the visible area first/entirely, with the main
  analysis content (Overview, tabs, Anomaly Detection) not reachable by a
  plain scroll — `.prism/runs/2026-08-07/mobile_scrolled.png`. Needs a
  dedicated mobile-nav investigation (possibly `st.columns()` stacking order
  on narrow viewports, or the hamburger/collapse control's actual target).
  Not touched this run — orthogonal to the two features shipped, and risky
  to patch blind without reproducing interactively across all tabs.
- [ok] The two new UI additions (Statistically Verified panel, Narrate
  anomalies button + result) use plain `st.button`/`st.container(border=True)`/
  `st.error`/`st.info` — they DO adapt correctly to both themes (verified in
  both light and dark screenshots) and don't inherit the dataframe/nav issues above.

## Verification performed
- 13 new unit tests, all passing (`pytest tests/ -q`).
- Existing regression suite `eval/autocleaner_eval.py`: 100% (8/8), unaffected.
- Manual Playwright smoke test against `samples/stock_data.csv`: Anomaly
  Detection → Find Anomalies → Narrate anomalies (error path shown correctly
  with a placeholder API key) and Auto Analyst → Run Full Analysis →
  Statistically Verified panel, both desktop (1440×1000) light + dark themes.
